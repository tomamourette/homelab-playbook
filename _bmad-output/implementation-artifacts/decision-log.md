# Implementation Decision Log

**Project:** homelab
**Started:** 2026-04-01
**Method:** Autonomous BMad execution with 90% confidence threshold

---

## Decision Format

| # | Date | Story | Decision | Confidence | Rationale | Outcome |
|---|------|-------|----------|------------|-----------|---------|

---

## Pre-Implementation Decisions

| # | Date | Context | Decision | Confidence | Rationale |
|---|------|---------|----------|------------|-----------|
| D-001 | 2026-04-01 | Epic structure | 6 epics following PRD deployment order: SMTP → Observability → Logging → SSO → Backup → Updates | 95% | Architecture explicitly specifies this sequence; each delivers standalone value |
| D-002 | 2026-04-01 | Story count | 22 stories initially, +2 from readiness review = 24 total | 95% | Readiness check found missing Diun ct-media-01 and Authelia middleware rollout stories |
| D-003 | 2026-04-01 | Diun ct-media-01 | Added Story 6.2 as Ansible role (not Docker stack) | 92% | Follows same pattern as promtail-media role; consistent with architecture cross-host pattern |
| D-004 | 2026-04-01 | Authelia rollout | Added Story 4.3 to modify all 17+ existing Compose files | 95% | Without this, existing services remain unprotected after SSO deployment |
| D-005 | 2026-04-01 | FR27 wording | Documented intentional divergence: PRD says "digest", architecture chose "separate emails" | 95% | User confirmed "separate emails" during architecture workflow; noted in Epic 6 |
| D-006 | 2026-04-01 | Parallel execution | 4 parallel pipelines after Epic 1: A(Obs→Log), B(SSO), C(Backup), D(Updates) | 93% | Epic 1 is the only hard prerequisite; other epics only need SMTP for email notifications |
| D-007 | 2026-04-01 | Cross-epic deps | Stories 5.4 and 6.5 depend on 2.1 (Grafana) for dashboard creation | 95% | Can't create dashboard panels without Grafana deployed; these stories deferred to phase 3 |
| D-008 | 2026-04-01 | Story 3.2/3.3 | Stories 3.2 and 3.3 can run in parallel (both depend on 3.1 only) | 90% | 3.2 deploys Promtail on ct-media-01; 3.3 configures Grafana Loki datasource; no file overlap |

---

## Implementation Decisions

_Entries added as stories are implemented._

| # | Date | Story | Decision | Confidence | Rationale | Outcome |
|---|------|-------|----------|------------|-----------|---------|
| D-101 | 2026-04-01 | 1.1 | Use boky/postfix:v4.3.0 as SMTP relay image | 95% | Lightweight Alpine-based Postfix relay, purpose-built for upstream relaying, well-maintained, env-var configurable | Implemented |
| D-102 | 2026-04-01 | 1.1 | Resource limits: 0.5 CPU, 128M memory | 92% | SMTP relay is lightweight, handles low-volume notification emails only; 128M ample for Postfix | Implemented |
| D-103 | 2026-04-01 | 1.1 | Add 6 specific Linux capabilities (NET_BIND_SERVICE, SETGID, SETUID, SYS_CHROOT, DAC_READ_SEARCH, KILL) | 93% | Postfix requires these for port binding, privilege dropping, chroot jails, queue access, and process management; minimal set needed | Implemented after review finding R1 |
| D-104 | 2026-04-01 | 1.1 | TCP-based health check instead of `postfix status` | 95% | Directly tests SMTP responsiveness at the protocol level; works reliably under restricted capabilities | Implemented after review finding R2 |
| D-105 | 2026-04-01 | 1.1 | Set hostname: smtp-relay on the container | 95% | Upstream SMTP providers may reject HELO/EHLO with container ID as hostname; explicit hostname is best practice | Implemented after review finding R3 |
| D-106 | 2026-04-01 | 1.2 | Add /var/log/mail volume mount for persistent Postfix log access | 93% | Allows operator to access mail.log on host filesystem at /opt/appdata/smtp-relay/log/ in addition to docker logs | Implemented |
| D-107 | 2026-04-01 | 1.2 | Create bash validation script with swaks/nc fallback | 95% | Provides repeatable test procedure; swaks is the gold standard for SMTP testing, nc fallback covers minimal environments | Implemented |
| D-108 | 2026-04-01 | 1.2 | Use docker logs for delivery failure visibility (not separate log aggregation) | 95% | boky/postfix routes Postfix logs to stdout by default; json-file driver with rotation already configured in 1.1; sufficient for FR31 | Implemented |
| D-400 | 2026-04-01 | 4.1 | Use authelia/authelia:4.38.18 as SSO provider image | 95% | Latest stable 4.38.x release; lightweight single-binary, YAML config, no PostgreSQL dependency per architecture decision | Implemented |
| D-401 | 2026-04-01 | 4.1 | Resource limits: 0.5 CPU, 256M memory | 93% | Authelia is lightweight Go binary; 256M provides headroom for argon2id password hashing which is memory-intensive | Implemented |
| D-402 | 2026-04-01 | 4.1 | Pass secrets via AUTHELIA_ environment variables, not config file | 95% | Authelia v4.38+ supports env var override with AUTHELIA_ prefix; keeps configuration.yml.sample committable without secret placeholders | Implemented after review finding R-401 |
| D-403 | 2026-04-01 | 4.1 | Use argon2id for password hashing with 64MB memory cost | 95% | Argon2id is Authelia's recommended algorithm; 64MB memory cost provides strong brute-force resistance for single-user homelab | Implemented |
| D-404 | 2026-04-01 | 4.1 | File-based session storage (no Redis) | 95% | Architecture decision: single-user, no HA requirement; file-based sessions persisted via /opt/appdata/authelia/ volume | Implemented |
| D-405 | 2026-04-01 | 4.1 | SMTP notifier with disable_require_tls and disable_starttls | 93% | Internal Docker network communication to smtp-relay:25; TLS unnecessary for localhost relay, and smtp-relay doesn't support STARTTLS | Implemented |
| D-600 | 2026-04-01 | 6.1 | Use crazymax/diun:4.28.0 as container image update notifier | 95% | PRD-specified tool; well-maintained, Docker socket provider, env-var configurable, lightweight Go binary | Implemented |
| D-601 | 2026-04-01 | 6.1 | Resource limits: 0.5 CPU, 256M memory | 93% | Diun needs memory for concurrent image digest comparisons (10 workers default); 256M provides headroom for large container fleets | Implemented |
| D-602 | 2026-04-01 | 6.1 | Configure Diun entirely via environment variables (no config file mount) | 95% | Diun v4 supports full env var configuration with DIUN_ prefix; avoids config file management complexity for basic operation | Implemented |
| D-603 | 2026-04-01 | 6.1 | No cap_add needed (unlike smtp-relay) | 95% | Diun runs as non-root Go binary, reads Docker socket only, binds no ports; cap_drop ALL is sufficient with no capabilities needed | Implemented |
| D-604 | 2026-04-01 | 6.1 | Data volume at /opt/appdata/update-checks/diun for state persistence | 95% | Diun stores its BoltDB database in /data; persisting across restarts prevents re-notifying for already-seen updates | Implemented |
| D-200 | 2026-04-01 | 2.1 | Use cAdvisor as primary metrics source for services without /metrics endpoints | 95% | cAdvisor auto-discovers all containers via Docker socket and collects CPU, memory, network, disk I/O — this covers FR7 for 30+ services without per-service scrape config | Implemented |
| D-201 | 2026-04-01 | 2.1 | Use literal IP 192.168.50.161 for ct-media-01 cross-host targets (not env vars) | 95% | Prometheus does not support shell-style env var substitution in prometheus.yml; IP sourced from stack-targets.yml | Implemented after review finding R-200 |
| D-202 | 2026-04-01 | 2.1 | Add cadvisor-media scrape job for ct-media-01 container metrics | 93% | FR7 requires metrics from ALL containers on BOTH hosts; without cAdvisor on ct-media-01, media services lack container-level metrics; scrape target added, cAdvisor deployment on ct-media-01 is a prerequisite | Implemented after review finding R-202 |
| D-203 | 2026-04-01 | 2.1 | Comment out Portainer scrape job — CE does not expose Prometheus-format metrics | 95% | Original config used /api/system/status which returns JSON, not Prometheus text format; Portainer is monitored via cAdvisor container metrics instead | Implemented after review finding R-203 |
| D-204 | 2026-04-01 | 2.1 | Add Grafana as scrape target on :3000/metrics | 95% | Grafana natively exposes Prometheus metrics; enables monitoring of dashboard query performance and datasource health | Implemented |
| D-205 | 2026-04-01 | 2.1 | Retain original replica: 'A' external label alongside new environment label | 92% | Original config included replica label for potential future dedup; safe to keep both labels | Implemented after review finding R-201 |
| D-500 | 2026-04-01 | 5.1 | Use Debian apt restic package (not manual binary) | 93% | Follows existing role conventions (docker-host uses apt for all tooling); apt handles updates via standard system maintenance | Implemented |
| D-501 | 2026-04-01 | 5.1 | Environment file at /etc/restic-backup.env instead of /opt/appdata path | 95% | Host-level agent (not containerized); /etc is standard location for system service config; mode 0600 root-only | Implemented |
| D-502 | 2026-04-01 | 5.1 | Conditional env sourcing in backup script (manual run vs systemd) | 93% | systemd EnvironmentFile sets vars automatically; script also supports manual execution by sourcing env file when vars are unset | Implemented after review finding R-500 |
| D-503 | 2026-04-01 | 5.1 | Remove `*.log` from default exclude patterns | 95% | Application logs in /opt/appdata/ may contain important state data; excluding them risks silent data loss on restore | Implemented after review finding R-501 |
| D-504 | 2026-04-01 | 5.1 | Add password validation assert task | 95% | Empty password default creates unencrypted-equivalent repo, violating NFR-SEC-2; fail-fast assert prevents misconfiguration | Implemented after review finding R-502 |
| D-505 | 2026-04-01 | 5.1 | Add TimeoutStartSec=7200 to service unit | 93% | NFR-PERF-3 requires backups complete within 2 hours; timeout prevents hung backup from blocking the timer indefinitely | Implemented after review finding R-503 |
| D-506 | 2026-04-01 | 5.2 | Use sendmail-first with nc fallback for SMTP delivery | 95% | sendmail is more reliable if present (handles dot-stuffing, queuing); nc fallback ensures notification works on minimal systems without a local MTA | Implemented |
| D-507 | 2026-04-01 | 5.2 | Use systemd template instance (restic-notify-failure@.service) with %n | 95% | Template instantiation passes failed unit name as %i, making the notification service reusable for any future OnFailure= consumer | Implemented |
| D-508 | 2026-04-01 | 5.2 | Add assert for restic_backup_notify_recipient when enabled | 95% | Empty recipient would silently succeed but send no email, defeating NFR-REL-1; fail-fast assert prevents misconfiguration | Implemented |
| D-509 | 2026-04-01 | 5.3 | Use templates/restic-restore.sh.j2 (Jinja2 template) instead of files/restic-check.sh (static file) | 95% | Script needs Jinja2 variables (restic_backup_env_file_path, restic_backup_bin) for consistency with restic-backup.sh.j2; static file would require hardcoded paths | Implemented |
| D-510 | 2026-04-01 | 5.3 | Combine list, restore, verify into one script with subcommands | 95% | Task spec explicitly requests subcommand pattern; single script reduces deployment surface and provides unified UX matching existing restic-backup.sh pattern | Implemented |
| D-511 | 2026-04-01 | 5.3 | Interactive safety confirmation before original-path restore | 93% | Restoring to original paths overwrites live data; confirmation prompt protects against accidental data loss during interactive use; skipped in non-interactive (piped) contexts | Implemented |
| D-605 | 2026-04-01 | 6.2 | SMTP host defaults to ct-docker-01 IP (192.168.50.194) for cross-host access | 92% | ct-media-01 has no local smtp-relay; architecture says Diun on each host watches its own Docker. SMTP relay binding must be updated from 127.0.0.1 to allow cross-host access (prerequisite). Follows same cross-host IP pattern as Promtail->Loki and Prometheus->cAdvisor-media | Implemented |
| D-606 | 2026-04-01 | 6.2 | Stagger schedule to 06:30 daily (30min after ct-docker-01 Diun at 06:00) | 95% | Avoids simultaneous registry API calls from both hosts; reduces risk of rate limiting from Docker Hub | Implemented |
| D-607 | 2026-04-01 | 6.2 | Container named diun-media (not diun) to avoid conflict if operator inspects both hosts | 95% | Distinct container name makes it clear which host's Diun is referenced in logs, notifications, and docker ps output | Implemented |
| D-608 | 2026-04-01 | 6.2 | No proxy network required (unlike ct-docker-01 Diun) | 95% | ct-docker-01 Diun joins proxy for Docker DNS access to smtp-relay:25; ct-media-01 Diun uses host IP directly, no Docker DNS resolution needed | Implemented |
| D-609 | 2026-04-01 | 6.2 | Env vars via template file instead of inline in docker-compose.yml.j2 | 93% | Ansible renders env file once; docker-compose uses env_file directive; separates secrets (SMTP config) from compose structure; env file is mode 0600 | Implemented |
| D-406 | 2026-04-01 | 4.2 | Define middleware via file provider (`authelia@file`) not Docker labels (`authelia@docker`) | 95% | Architecture file map explicitly specifies `config/dynamic/authelia-middleware.yml` as NEW file; file provider decouples middleware from container lifecycle; consistent with existing security-headers@file pattern | Implemented |
| D-407 | 2026-04-01 | 4.2 | Mount dynamic/ subdirectory rather than individual file | 95% | Traefik v3 file provider recursively scans directory tree; subdirectory mount allows adding future middleware configs without docker-compose changes | Implemented |
| D-408 | 2026-04-01 | 4.2 | No changes needed to traefik.yml static config | 95% | File provider already configured with `directory: /etc/traefik` and `watch: true`; recursive scan auto-discovers `/etc/traefik/dynamic/` subdirectory | Implemented |
| D-409 | 2026-04-01 | 4.3 | Place `authelia@file` first in middleware chains | 95% | Auth check must run before other middleware (e.g., security-headers, CORS); Traefik processes middleware in declaration order; unauthenticated requests should be redirected before any other processing | Implemented |
| D-410 | 2026-04-01 | 4.3 | Include observability stack (prometheus, grafana) in middleware rollout | 95% | Even though created in Story 2.1 (this sprint), these are persistent services with web UIs that must be SSO-protected per FR20; not ephemeral like smtp-relay/diun | Implemented |
| D-411 | 2026-04-01 | 4.3 | Keep authelia@file on Plex despite its own auth | 92% | Plex has built-in auth, but FR20 says ALL services redirected to login; operator can add Authelia bypass rule for Plex in access_control if double-auth is undesirable | Implemented |
| D-206 | 2026-04-01 | 2.2 | Separate dashboards/ directory from provisioning config directory | 95% | ARCH-8 says "bind-mount provisioning directory"; epics spec shows `dashboards/` as a separate directory. Cleaner separation: provisioning YAML in config/grafana/provisioning/, dashboard JSON in dashboards/. New volume mount `./dashboards:/var/lib/grafana/dashboards:ro` | Implemented |
| D-207 | 2026-04-01 | 2.2 | Use cAdvisor container_last_seen for running status instead of Docker API probe | 95% | cAdvisor only reports metrics for running containers; presence of container_last_seen == container is running. No need for blackbox probes. Stopped containers simply absent from the table. | Implemented |
| D-208 | 2026-04-01 | 2.2 | Use `changes()` function for restart count instead of `increase(container_start_time_seconds)` | 95% | `increase()` on a timestamp metric returns the timestamp delta (huge number), not restart count. `changes()` counts how many times the value changed in the window — exactly what we need for restart detection. | Implemented after review finding R-204 |
| D-209 | 2026-04-01 | 2.2 | Use datasource variable `${DS_PROMETHEUS}` with hidden template var | 95% | Standard Grafana pattern for provisioned dashboards; allows datasource to be resolved dynamically if renamed; hide=2 keeps it invisible to users | Implemented |
| D-210 | 2026-04-01 | 2.2 | Dashboard title "Service Health Overview" follows architecture naming "{Domain} Overview" | 95% | Architecture pattern specifies domain-based dashboard naming; "Service Health" is the domain, "Overview" is the suffix | Implemented |
| D-610 | 2026-04-01 | 6.3 | Default SMTP host 192.168.50.194 (not localhost) for apt-check role | 95% | Role targets ALL containers and Proxmox nodes, not just ct-docker-01; most hosts lack local smtp-relay binding; cross-host IP is the safe default (same pattern as D-605 for Diun-media) | Implemented |
| D-611 | 2026-04-01 | 6.3 | Schedule apt-check at 05:00 daily with 300s random delay | 95% | Staggered from Diun (06:00/06:30) and restic-backup (02:00); avoids overlapping I/O from apt cache refresh; 300s random delay prevents thundering herd across multiple hosts | Implemented |
| D-612 | 2026-04-01 | 6.3 | No email sent when zero packages upgradable (suppress noise) | 95% | Acceptance criteria explicitly states "produces no email if there are no updates (no noise)"; script exits 0 silently when UPGRADE_COUNT is 0 | Implemented |
| D-613 | 2026-04-01 | 6.3 | Omit ProtectSystem from service unit — apt-get update needs /var/lib/apt write | 95% | restic-backup uses ProtectSystem=full because it only reads /opt/appdata; apt-check must write to /var/lib/apt/lists and /var/cache/apt; PrivateTmp + NoNewPrivileges still applied | Implemented after review finding R-609 |
| D-614 | 2026-04-01 | 6.3 | Use sendmail-first with nc fallback for SMTP delivery | 95% | Same proven pattern as restic-notify-failure (D-506); consistent across all host-level notification scripts | Implemented |
| D-615 | 2026-04-01 | 6.3 | TimeoutStartSec=600 (10 minutes) for apt-check service | 93% | apt update on slow mirrors can take several minutes; 10 minutes is generous but prevents indefinite hangs; lower than restic-backup's 7200s because apt operations are lightweight | Implemented |
| D-300 | 2026-04-01 | 3.1 | Use grafana/loki:3.4.2 as log aggregation engine | 95% | Latest stable Loki 3.x; single-binary filesystem mode per architecture decision; native Grafana integration via LogQL | Implemented |
| D-301 | 2026-04-01 | 3.1 | Use grafana/promtail:3.4.2 matching Loki version | 95% | Version-matched with Loki for protocol compatibility; Docker socket auto-discovery for FR18 | Implemented |
| D-302 | 2026-04-01 | 3.1 | Loki single-binary mode with TSDB schema v13 and filesystem storage | 95% | Architecture specifies single-binary filesystem mode; TSDB v13 is the current recommended schema for Loki 3.x; no external dependencies | Implemented |
| D-303 | 2026-04-01 | 3.1 | Compactor-based retention (not table_manager) for 30-day cleanup | 95% | table_manager is deprecated in Loki 3.x; compactor retention is the supported approach; 720h = 30 days per architecture spec | Implemented |
| D-304 | 2026-04-01 | 3.1 | Resource limits: Loki 512M/1.0 CPU, Promtail 128M/0.5 CPU | 93% | Loki needs memory for index caching and query processing (32 containers, 30-day retention); Promtail is lightweight tail-and-forward agent; both configurable via env vars | Implemented |
| D-305 | 2026-04-01 | 3.1 | Loki internal only on monitoring network (no Traefik labels, no proxy network) | 95% | Architecture explicitly says "loki :3100 (internal only, Grafana datasource)"; no external access needed; Grafana queries Loki via Docker monitoring network | Implemented |
| D-306 | 2026-04-01 | 3.1 | Promtail depends_on loki with condition: service_healthy | 95% | Promtail must not start pushing before Loki is ready to ingest; healthy condition uses /ready endpoint; prevents log loss during stack startup | Implemented |
| D-307 | 2026-04-01 | 3.1 | Promtail cap_add DAC_READ_SEARCH for Docker socket access | 93% | Docker socket is root-owned; Promtail runs without explicit user directive; DAC_READ_SEARCH allows reading files/directories bypassing DAC permission checks; minimal capability for socket read access | Implemented after review finding R-300 |
| D-308 | 2026-04-01 | 3.1 | Promtail host label set to "ct-docker-01" in relabel config | 95% | Multi-host identification label for cross-host log queries when ct-media-01 Promtail is added in Story 3.2; hardcoded in config (not env var) because config file is host-specific | Implemented |
| D-622 | 2026-04-01 | 6.5 | Use Prometheus textfile collector (ARCH-7) for apt-check and tool-check metrics, matching restic-backup pattern from Story 5.4 | 95% | Proven pattern; node-exporter textfile collector already configured; consistent with backup metrics approach | Implemented |
| D-623 | 2026-04-01 | 6.5 | Use Loki LogQL panel for Diun container image updates instead of creating a separate metrics-extraction script | 92% | Diun has no native Prometheus metrics; BoltDB is binary and not scriptable from bash; Promtail already collects Diun logs; LogQL provides actual image names/tags (more useful than a count); avoids new timer/script/role | Implemented |
| D-624 | 2026-04-01 | 6.5 | Hybrid datasource dashboard (Prometheus + Loki) — Prometheus for structured apt/tool metrics, Loki for Diun logs | 93% | Each data source uses the best tool: textfile collector for structured counts, Loki for semi-structured log messages; both datasources already provisioned in Grafana | Implemented |
| D-625 | 2026-04-01 | 6.5 | Write textfile metrics before email send attempts — metrics should reflect check results regardless of notification delivery | 95% | Email delivery is best-effort; Prometheus metrics should always be updated when the check completes; decouples observability from notification | Implemented |
| D-626 | 2026-04-01 | 6.5 | apt-check failure handler preserves previous upgrade count from existing .prom file | 95% | Same pattern as restic-backup preserving last_success_timestamp on failure; avoids resetting metrics to 0 on transient apt failures | Implemented |
| D-627 | 2026-04-01 | 6.5 | Dashboard UID `homelab-update-overview` follows existing pattern (`homelab-backup-overview`, `homelab-service-health`) | 95% | Consistent naming across all dashboards | Implemented |
| D-315 | 2026-04-01 | 3.3 | Use `isDefault: false` for Loki datasource | 95% | Prometheus is the primary datasource for dashboards and alerts; Loki is secondary for log exploration; operator selects Loki explicitly in Explore panel | Implemented |
| D-316 | 2026-04-01 | 3.3 | Set maxLines to 1000 (not default 5000) | 93% | Balances query performance with result completeness; 1000 lines sufficient for most debugging sessions; operator can override per-query in Explore UI | Implemented |
| D-317 | 2026-04-01 | 3.3 | Set timeout to 60 seconds | 95% | NFR-PERF-2 targets 5s for 30-day queries but complex regex filters may take longer; 60s provides headroom without leaving connections hanging indefinitely | Implemented |
| D-318 | 2026-04-01 | 3.3 | Add derivedFields for container_name correlation | 92% | Enables click-through from log lines to related container context; matches container_name label set by Promtail relabel config in Story 3.1 | Implemented |
| D-319 | 2026-04-01 | 3.3 | No custom log exploration dashboard created -- use Grafana Explore | 95% | Grafana Explore is purpose-built for ad-hoc log exploration with LogQL; architecture and PRD both reference "Grafana Explore panel using LogQL" not a custom dashboard | Implemented |
| D-320 | 2026-04-01 | 3.4 | Create validation script (consistent with D-217, D-412) despite epics saying "no new files -- validation only" | 95% | Same pattern as Stories 2.4 and 4.4; script provides repeatable automated verification of all FR14-FR18 checks | Implemented |
| D-321 | 2026-04-01 | 3.4 | 8 checks covering all 5 FRs with both runtime and static fallback | 95% | Each FR mapped to specific checks; runtime checks validate live pipeline on production host; static analysis validates config correctness on development machine | Implemented |
| D-322 | 2026-04-01 | 3.4 | SKIP for runtime checks when containers unavailable (same as D-219, D-413) | 95% | Matches established pattern; 3 skipped checks (loki-health, loki-has-data, logql-query) require running containers; will show PASS on production host | Implemented |
| D-323 | 2026-04-01 | 3.4 | Query {job="docker"} for loki-has-data check | 95% | Matches Promtail scrape_configs job_name in both ct-docker-01 config and ct-media-01 Ansible template; broadest possible label match | Implemented |

---

## Escalated to User

_Decisions where confidence was below 90% and user input was requested._

| # | Date | Story | Question | User Response |
|---|------|-------|----------|---------------|

---

## Review Findings Log

_Issues found during code review and their resolution._

| # | Date | Story | Finding | Severity | Resolution |
|---|------|-------|---------|----------|------------|
| R-101 | 2026-04-01 | 1.1 | cap_drop ALL with no cap_add prevents Postfix from starting | High | Added 6 targeted capabilities: NET_BIND_SERVICE, SETGID, SETUID, SYS_CHROOT, DAC_READ_SEARCH, KILL |
| R-102 | 2026-04-01 | 1.1 | Health check `postfix status` unreliable under restricted capabilities | Medium | Changed to TCP-based SMTP check via nc |
| R-103 | 2026-04-01 | 1.1 | Missing hostname causes container ID in HELO/EHLO | Medium | Added hostname: smtp-relay |
| R-104 | 2026-04-01 | 1.1 | start_period too short at 15s | Low | Increased to 30s |
| R-105 | 2026-04-01 | 1.2 | Log volume /var/log:rw overly broad, could conflict with container rsyslog | Medium | Scoped to /var/log/mail:rw for Postfix-specific logs only |
| R-106 | 2026-04-01 | 1.2 | Validation script grep uses $recipient as regex, breaks on metacharacters | Medium | Changed to grep -F for fixed-string matching |
| R-107 | 2026-04-01 | 1.2 | nc fallback sends LF-only line endings, SMTP requires CRLF (RFC 5321) | Medium | Replaced echo with printf using explicit \r\n endings |
| R-108 | 2026-04-01 | 1.2 | nc availability not verified before use in status subcommand | Low | Accepted — nc is standard on Debian/Ubuntu target hosts |
| R-401 | 2026-04-01 | 4.1 | Session secret and storage encryption key hardcoded as placeholders in configuration.yml.sample | High | Removed secret values from config sample; secrets injected via AUTHELIA_ environment variables in docker-compose.yml |
| R-402 | 2026-04-01 | 4.1 | Authelia portal router missing explicit warning about not adding authelia@docker middleware | Medium | Added comment on Traefik router labels warning against infinite redirect loop |
| R-403 | 2026-04-01 | 4.1 | Access control wildcard *.bi-services.be does not match bare domain bi-services.be | Medium | Added explicit rule for bare domain bi-services.be before wildcard rule |
| R-404 | 2026-04-01 | 4.1 | Missing expose directive for port 9091 | Low | Accepted — Docker Compose services on same network communicate without expose; adding it is cosmetic only |
| R-601 | 2026-04-01 | 6.1 | Health check `diun --version` does not validate service readiness | Medium | Changed to file-based check (`test -f /data/diun.db`); Diun creates its BoltDB after first successful run, proving the serve process initialized. Fallback to `diun --version` for pre-first-run. Increased interval to 60s and start_period to 60s since Diun is a scheduler not a long-running server. |
| R-602 | 2026-04-01 | 6.1 | Missing `DIUN_PROVIDERS_DOCKER_WATCHBYDEFAULT=true` env var means only labeled containers are watched | High | Added `DIUN_PROVIDERS_DOCKER_WATCHBYDEFAULT=true` to docker-compose.yml; without this, FR24 (check ALL running containers) would fail since new containers lack `diun.enable=true` labels |
| R-603 | 2026-04-01 | 6.1 | diun.yml.sample sets `watchByDefault: true` and `watchStopped: true` redundantly with env vars | Medium | Commented out provider settings in sample config; clarified that env vars in docker-compose.yml take precedence and config file is for advanced overrides only |
| R-200 | 2026-04-01 | 2.1 | Initial implementation used `${ENV_VAR:-default}` syntax in prometheus.yml — Prometheus does not support shell env var substitution | High | Replaced with literal IP addresses from stack-targets.yml (ct-media-01: 192.168.50.161) |
| R-201 | 2026-04-01 | 2.1 | Original `replica: 'A'` external label was removed; may be needed for future dedup or remote-write scenarios | Low | Restored `replica: 'A'` alongside new `environment: 'production'` label |
| R-202 | 2026-04-01 | 2.1 | ct-media-01 had no cAdvisor scrape target — FR7 requires metrics from ALL containers on BOTH hosts; 9+ media containers would lack container-level metrics | Medium | Added cadvisor-media scrape job targeting 192.168.50.161:8080; documented that cAdvisor must be deployed on ct-media-01 as a prerequisite |
| R-203 | 2026-04-01 | 2.1 | Original Portainer scrape job used `/api/system/status` which returns JSON, not Prometheus text format; changed to `/api/metrics` which also does not exist in Portainer CE | Medium | Commented out Portainer scrape job entirely; Portainer monitored via cAdvisor container metrics instead |
| R-500 | 2026-04-01 | 5.1 | Backup script double-loads env (EnvironmentFile in service unit AND source in script) | Medium | Changed script to conditionally source env file only when RESTIC_REPOSITORY/RESTIC_PASSWORD are not already set; supports both systemd and manual execution |
| R-501 | 2026-04-01 | 5.1 | Default exclude patterns include `*.log` which may silently drop important application state | High | Removed `*.log` from restic_backup_exclude_patterns default; operator can re-add if certain their logs are transient |
| R-502 | 2026-04-01 | 5.1 | Empty default for restic_backup_password allows creating unencrypted repo (violates NFR-SEC-2) | High | Added assert task at top of tasks/main.yml that fails if password is empty string |
| R-503 | 2026-04-01 | 5.1 | Service unit missing timeout — hung backup could block timer indefinitely | Medium | Added TimeoutStartSec=7200 (2 hours, matching NFR-PERF-3 backup completion target) |
| R-504 | 2026-04-01 | 5.1 | `set -e` in backup script causes premature exit before structured error logging | Medium | Changed to `set -uo pipefail` (removed -e); script handles errors explicitly via if/else and BACKUP_FAILED flag |
| R-405 | 2026-04-01 | 4.2 | Architecture examples show `authelia@docker` but file provider requires `authelia@file` reference | Medium | Documented in D-406; Story 4.3 must use `authelia@file` (not `authelia@docker`) when adding middleware labels to service routers. Architecture example is illustrative, file map is authoritative. |
| R-204 | 2026-04-01 | 2.2 | Restart count used `increase(container_start_time_seconds)` which returns timestamp delta not restart count | Medium | Changed to `count(changes(container_start_time_seconds{name!=""}[24h]) > 0)` which correctly counts containers that restarted |
| R-205 | 2026-04-01 | 2.2 | Table panel used `job` label for Host column; `job` shows "cadvisor"/"cadvisor-media" not host names | Medium | Changed to use `host` label from scrape config which shows "ct-docker-01"/"ct-media-01"; excluded `job`, `instance`, and Docker Compose labels from table display |
| R-505 | 2026-04-01 | 5.2 | OnFailure= directive initially placed in [Service] section — systemd ignores it there; must be in [Unit] | High | Moved OnFailure=restic-notify-failure@%n.service to [Unit] section |
| R-406 | 2026-04-01 | 4.3 | Traefik dashboard has triple middleware chain (authelia@file + traefik-auth + security-headers@file) | Low | Accepted -- defense in depth; Authelia SSO runs first, then basic auth provides a second factor specific to Traefik dashboard, then security headers are applied. Operator can remove traefik-auth if SSO is sufficient. |
| R-407 | 2026-04-01 | 4.3 | Obsidian CORS middleware must remain after authelia@file for LiveSync plugin compatibility | Medium | Placed authelia@file before obsidian-cors in comma-separated list; CORS headers are applied after auth check passes; verified middleware order is correct for authenticated CouchDB sync requests. |
| R-408 | 2026-04-01 | 4.3 | Plex and media services have their own authentication but now also require Authelia SSO | Low | Accepted per FR20 requirement; double-auth may be slightly inconvenient but ensures consistent SSO posture; operator can add Authelia access_control bypass rules for specific services if desired. |
| R-506 | 2026-04-01 | 5.2 | Fractional sleep values (0.5s) in nc SMTP function may fail on some systems; journal log lines starting with '.' could terminate DATA prematurely | Medium | Changed to integer sleeps; added RFC 5321 dot-stuffing via sed |
| R-605 | 2026-04-01 | 6.2 | smtp-relay bound to 127.0.0.1:25 (ARCH-5) -- ct-media-01 Diun cannot reach it cross-host | High | Default SMTP host set to ct-docker-01 IP (192.168.50.194); documented prerequisite to update smtp-relay binding for cross-host access |
| R-606 | 2026-04-01 | 6.2 | Initial schedule used invalid 6-field cron; Diun uses standard 5-field cron | Medium | Fixed to `30 6 * * *` (06:30 daily, staggered 30min after ct-docker-01 Diun) |
| R-607 | 2026-04-01 | 6.2 | No proxy network in docker-compose.yml.j2 unlike ct-docker-01 Diun | Low | By design -- ct-media-01 has no local smtp-relay on proxy network; Diun-media uses host IP for cross-host SMTP; default bridge network sufficient |
| R-614 | 2026-04-01 | 6.5 | Initial Loki query used `container_name` label but Promtail relabel config maps to `container` (stripped leading `/`) | Medium | Changed LogQL selector from `{container_name=~"diun|diun-media"}` to `{container=~"diun|diun-media"}` to match actual Promtail label mapping |
| D-211 | 2026-04-01 | 2.3 | Use Grafana unified alerting (v10+) with provisioned contact points and notification policies, not legacy notification channels | 95% | Grafana 10.x defaults to unified alerting; legacy alerting is deprecated and disabled. Unified alerting supports provisioning via YAML in /etc/grafana/provisioning/alerting/ | Implemented |
| D-212 | 2026-04-01 | 2.3 | Use GF_SMTP_STARTTLS_POLICY=NoStartTLS and GF_SMTP_SKIP_VERIFY=true for internal smtp-relay | 95% | Internal Docker network to smtp-relay:25; TLS unnecessary and smtp-relay does not support STARTTLS (same rationale as D-405 for Authelia) | Implemented |
| D-213 | 2026-04-01 | 2.3 | Deprecate legacy config/rules/homelab-alerts.yml (empty groups) to prevent duplicate alerts | 95% | New alerting/ directory supersedes old rules/ with properly named {Domain}{Condition} alerts; keeping both active would cause duplicate alert firings | Implemented |
| D-214 | 2026-04-01 | 2.3 | UnexpectedRestart uses `changes(container_start_time_seconds) > 1` in 15m window (not > 0) | 93% | changes() == 1 fires on every fresh container start; > 1 means the container restarted at least once after its initial start. 15m window long enough to detect restarts but short enough to suppress operator-initiated `docker compose up -d` cycles | Implemented |
| D-215 | 2026-04-01 | 2.3 | Hardcode recipient email in contactpoints.yml (not env var) | 95% | Grafana provisioning YAML does not support environment variable substitution; operator must edit contactpoints.yml directly. Documented in .env.sample | Implemented |
| D-216 | 2026-04-01 | 2.3 | Group alerts by alertname + host with 30s wait, 5m interval, 4h repeat | 92% | 30s group_wait aggregates simultaneous alerts into one email; 5m group_interval prevents email flooding during cascading failures; 4h repeat_interval balances persistence without spam | Implemented |
| R-206 | 2026-04-01 | 2.3 | UnexpectedRestart `changes() > 1` threshold means single restarts (changes==1) are not alerted | Low | Accepted -- changes()==1 represents a normal fresh start (container_start_time_seconds changes once when container starts). A true restart produces changes()>=2 (stop+start). This avoids false positives on initial container creation. |
| R-207 | 2026-04-01 | 2.3 | Legacy rules/homelab-alerts.yml overlaps with new alerting/ rules (HostHighCpuLoad vs ContainerHighCPU, HostOutOfDiskSpace vs DiskSpaceCritical, ContainerKilled vs ServiceDown) | High | Emptied legacy rules file (groups: []) to prevent duplicate alerts. New alerting/ rules follow {Domain}{Condition} naming convention and have better-tuned thresholds. |
| R-208 | 2026-04-01 | 2.3 | Grafana provisioning YAML does not support env var substitution for contact point email address | Medium | Hardcoded placeholder email in contactpoints.yml with comment instructing operator to edit; cross-referenced in .env.sample |
| R-209 | 2026-04-01 | 2.3 | DiskSpaceWarning uses `< 20` (80% full) not `> 80` to match node_filesystem_avail_bytes percentage calculation (avail/total * 100 gives free%, not used%) | Low | Correct -- the expr calculates percentage free, so `< 20` means "less than 20% free" which equals ">80% used". Verified against node-exporter metric semantics. |
| R-507 | 2026-04-01 | 5.3 | Snapshot validation via `grep -q '"id"'` is fragile -- `restic snapshots <id> --json` returns `[]` for unknown snapshots, not a grep-matchable error | Medium | Changed to explicit check for empty/null/`[]` JSON output; avoids false positives from grep matching against unrelated JSON content |
| R-608 | 2026-04-01 | 6.3 | `apt list --upgradable` header line varies ("Listing..." vs "Listing...Done") across Debian/Ubuntu versions | Low | Changed grep filter from `^Listing\.\.\.` to `^Listing` to match all variants |
| R-609 | 2026-04-01 | 6.3 | ProtectSystem=full would break apt-get update (needs /var/lib/apt write access) | Medium | Intentionally omitted ProtectSystem from service unit; only PrivateTmp and NoNewPrivileges applied |
| R-610 | 2026-04-01 | 6.3 | Proxmox nodes may lack sendmail or nc | Low | Accepted -- same pattern as R-108; Proxmox VE ships ncat via nmap package; Debian containers have netcat-openbsd |
| D-412 | 2026-04-01 | 4.4 | Create validation script rather than "no new files" as specified in epics | 95% | Epics say "No new files -- validation only" but the task brief explicitly requests a validation script; script provides repeatable, automated verification of all FR19-FR23 checks | Implemented |
| D-413 | 2026-04-01 | 4.4 | Script gracefully degrades when network/Docker unavailable (SKIP vs FAIL) | 95% | Validation script must work both on the development machine (static analysis of compose files) and on the live host (runtime checks via curl/docker); SKIP for unreachable checks prevents false failures | Implemented |
| D-414 | 2026-04-01 | 4.4 | Accept HTTP 401 as valid redirect behavior (not just 302) | 93% | Authelia forwardAuth may return 401 instead of 302 depending on Traefik version and client Accept headers; both indicate active SSO protection | Implemented |
| R-409 | 2026-04-01 | 4.4 | Middleware coverage check confirmed 20/21 routers protected; authelia-secure correctly excluded | N/A | Static analysis of all compose files verified complete coverage; no gaps found; authelia self-exclusion is by design (R-402/R-415 infinite redirect prevention) |
| D-512 | 2026-04-01 | 5.4 | Enable node-exporter textfile collector with `--collector.textfile.directory=/textfile` and mount `/opt/appdata/observability/node-exporter/textfile:/textfile:ro` | 95% | ARCH-7 requires textfile collector for backup metrics; node-exporter reads .prom files from this directory; read-only mount since only host scripts write to it | Implemented |
| D-513 | 2026-04-01 | 5.4 | Write 5 Prometheus metrics after each backup: last_status, last_success_timestamp, last_duration_seconds, last_size_bytes, snapshots_total | 95% | Covers all FR13 requirements (last run, success/failure, size) plus duration and snapshot count for dashboard completeness; all prefixed `restic_backup_` | Implemented |
| D-514 | 2026-04-01 | 5.4 | Add `restic_backup_textfile_dir` and `restic_backup_textfile_file` to defaults/main.yml | 95% | Configurable path follows existing role variable conventions; default `/opt/appdata/observability/node-exporter/textfile` matches node-exporter volume mount | Implemented |
| D-515 | 2026-04-01 | 5.4 | Atomic .prom file write via temp file + mv | 95% | Prevents node-exporter from reading a partial file during write; standard pattern for textfile collector | Implemented |
| D-516 | 2026-04-01 | 5.4 | Dashboard title "Backup Overview" with uid `homelab-backup-overview` | 95% | Architecture naming convention: `{Domain} Overview`; uid follows `homelab-{domain}` pattern from service-health dashboard | Implemented |
| D-517 | 2026-04-01 | 5.4 | Dashboard refresh interval 5m (vs 30s for service-health) | 95% | Backup metrics update once per nightly run; 5m refresh reduces unnecessary Prometheus queries while still reflecting changes within minutes | Implemented |
| D-518 | 2026-04-01 | 5.4 | Add Ansible task to create textfile collector directory in restic-backup role | 95% | Directory must exist before backup script writes to it; `mkdir -p` in script is fallback but Ansible task ensures correct ownership/permissions at deployment time | Implemented |
| R-508 | 2026-04-01 | 5.4 | Last success timestamp inline command substitution in heredoc was fragile | Medium | Pre-computed success_ts variable before heredoc; on failure reads previous value from existing .prom file; cleaner and more debuggable |
| R-509 | 2026-04-01 | 5.4 | `restic stats latest --json` output format may vary across restic versions | Low | Accepted -- grep+cut parsing with `|| true` fallback gracefully degrades to 0 bytes; operator sees correct status/duration regardless |
| D-616 | 2026-04-01 | 6.4 | Added explicit `binary` field to tool config to decouple display name from command name | 95% | Node.js tool is named "nodejs" for display but the binary is "node"; using a `binary` field avoids fragile string manipulation (`.replace()`, `.split()`) in Jinja2 templates | Implemented |
| D-617 | 2026-04-01 | 6.4 | Changed Node.js latest version source from resolve.nodejs.org/lts to GitHub API releases/latest | 95% | resolve.nodejs.org/lts returns a redirect/text that may change format; GitHub API provides consistent JSON response matching the same pattern used for Terraform, Ansible, and Docker | Implemented |
| D-618 | 2026-04-01 | 6.4 | TimeoutStartSec=300 (5 minutes) for tool-check service | 95% | Each tool's curl has --max-time 30s; with 4 tools that's 2 minutes worst case for API calls plus version command execution; 5 minutes provides generous headroom without risk of indefinite hang | Implemented |
| D-217 | 2026-04-01 | 2.4 | Create validation script (same pattern as Story 4.4) despite epics saying "no new files -- validation only" | 95% | Consistent with D-412; script provides repeatable automated verification of all FR7-FR12 checks; matches validate-sso.sh pattern | Implemented |
| D-218 | 2026-04-01 | 2.4 | 9 checks instead of 6 specified in task brief (added prometheus-targets, node-exporter-metrics, cadvisor-metrics) | 95% | FR7 explicitly requires metrics from ALL containers and hosts; separate checks verify each metrics source independently | Implemented |
| D-219 | 2026-04-01 | 2.4 | SKIP (not FAIL) when container not found but docker CLI is available | 95% | Matches validate-sso.sh graceful degradation pattern (D-413); dev machine has docker but no production containers; SKIP prevents false failures | Implemented |
| R-210 | 2026-04-01 | 2.4 | prometheus-targets grep-based JSON parsing is fragile for extracting down target names | Low | Accepted -- no jq dependency required; worst case shows "unknown" for target names; status counts (UP/DOWN) are reliable via simple grep |
| R-211 | 2026-04-01 | 2.4 | smtp-reachable check uses wget spider which always fails at HTTP level on SMTP port 25 | Low | Expected behavior -- TCP connection success is detected from wget error output; SMTP is not HTTP so wget "failure" with connection is actually a PASS |
| D-619 | 2026-04-01 | 6.4 | Schedule tool-check at 05:30 daily, staggered 30min after apt-check (05:00) | 95% | Avoids overlapping with apt-check (05:00) and Diun (06:00/06:30); spreads notification load on smtp-relay; same 300s random delay as apt-check | Implemented |
| D-620 | 2026-04-01 | 6.4 | Default SMTP host 192.168.50.194 (not localhost) for tool-version-check role | 95% | Same rationale as D-610 for apt-check: role targets ALL hosts including ct-media-01 and Proxmox nodes which lack local smtp-relay; cross-host IP is safe default | Implemented |
| D-621 | 2026-04-01 | 6.4 | Gracefully skip tools that are not installed rather than fail | 95% | Not all hosts have all tools (e.g., Proxmox nodes may lack Node.js, containers may lack Terraform); skipping with log message is the correct behavior for a multi-host role | Implemented |
| R-611 | 2026-04-01 | 6.4 | Initial template used fragile Jinja2 `name.split('-')[0]` and `name.replace('nodejs','node')` for command existence check | Medium | Replaced with explicit `binary` field in defaults/main.yml and `tool.binary \| default(tool.name)` in template; cleaner and extensible for operator-added tools |
| R-612 | 2026-04-01 | 6.4 | Node.js resolve.nodejs.org/lts URL response format is undocumented and may not return a simple parseable version string | Medium | Changed to GitHub API `repos/nodejs/node/releases/latest` for consistency with other tools; all 4 tools now use the same GitHub API pattern |
| R-613 | 2026-04-01 | 6.4 | `grep -oP` (PCRE) required for JSON key extraction without jq dependency | Low | Accepted -- target hosts are Debian/Ubuntu-based (GNU grep with PCRE support available via libpcre); Proxmox VE also includes GNU grep. Adding jq dependency would increase role complexity for marginal benefit. |
| R-300 | 2026-04-01 | 3.1 | Promtail needs DAC_READ_SEARCH capability to read Docker socket | Medium | Added cap_add: DAC_READ_SEARCH; Docker socket is root-owned, Promtail runs without explicit user; same pattern as smtp-relay capabilities (D-103) |
| R-301 | 2026-04-01 | 3.1 | Loki user directive "${PUID}:${PGID}" requires /loki volume to be owned by same UID | Low | Consistent with prometheus and grafana in same stack; operator must ensure /opt/appdata/observability/loki/ ownership matches PUID:PGID |
| R-302 | 2026-04-01 | 3.1 | Promtail has no user directive (unlike Loki) -- needed for Docker socket access | Low | By design -- Docker socket typically root-owned; running as container default user with DAC_READ_SEARCH is the minimal-privilege approach for socket reading |
| R-303 | 2026-04-01 | 3.1 | .env.sample file is permission-restricted; cannot add LOKI_VERSION, PROMTAIL_VERSION, LOKI_DATA, PROMTAIL_POSITIONS vars programmatically | Medium | Documented required env vars in story file; operator must manually add to .env.sample: LOKI_VERSION=3.4.2, PROMTAIL_VERSION=3.4.2, LOKI_DATA=/opt/appdata/observability/loki, PROMTAIL_POSITIONS=/opt/appdata/observability/promtail |
| R-310 | 2026-04-01 | 3.3 | Grafana provisioning directory already mounted read-only in docker-compose.yml (Story 2.1) -- no Compose modifications needed | N/A | Confirmed; new YAML file in datasources/ directory is automatically discovered on Grafana restart |
| R-311 | 2026-04-01 | 3.3 | Prometheus datasource has `editable: true` -- Loki should match for consistency | Low | Set `editable: true` on Loki datasource to match existing Prometheus pattern; allows operator to tweak settings via UI if needed |
| R-312 | 2026-04-01 | 3.3 | Loki datasource URL uses Docker service name `loki` -- requires both services on same Docker network | N/A | Confirmed; both Grafana and Loki are on the `monitoring` network per docker-compose.yml |
| R-307 | 2026-04-01 | 3.4 | SKIP detail messages concatenate multiple items without delimiters | Low | Accepted -- matches validate-observability.sh pattern; individual check details are space-separated; changing would break consistency with existing script |
| D-309 | 2026-04-01 | 3.2 | Expose Loki port 3101 on ct-docker-01 host for cross-host Promtail ingestion | 95% | Loki was internal-only on Docker monitoring bridge network; ct-media-01 Promtail needs HTTP access to push logs cross-host. Port 3101 chosen to avoid conflict with container-internal 3100. Configurable via LOKI_BIND_ADDRESS and LOKI_EXTERNAL_PORT env vars | Implemented |
| D-310 | 2026-04-01 | 3.2 | Follow diun-media role pattern for promtail-media role structure | 95% | Consistent with ARCH-6; same task flow: create dirs, template config + compose, pull image, compose up, health check. Proven pattern from Story 6.2 | Implemented |
| D-311 | 2026-04-01 | 3.2 | Use identical promtail-config.yml as 3.1 but with templated host label and Loki URL | 95% | Same Docker SD config, relabel rules, and pipeline stages ensure consistent log labeling across both hosts; only difference is host label (ct-media-01 vs ct-docker-01) and Loki endpoint (cross-host IP vs Docker DNS) | Implemented |
| D-312 | 2026-04-01 | 3.2 | DAC_READ_SEARCH capability on promtail-media container (same as 3.1) | 95% | Docker socket is root-owned; same rationale as R-300. Required for Docker SD config to enumerate containers | Implemented |
| D-313 | 2026-04-01 | 3.2 | Health check uses wget to /ready endpoint (same as 3.1 Promtail) | 95% | Promtail exposes /ready on :9080; wget is available in Promtail image; consistent with 3.1 conventions | Implemented |
| D-314 | 2026-04-01 | 3.2 | Default Loki URL uses port 3101 (host-mapped) not 3100 (container-internal) | 95% | Cross-host communication bypasses Docker networks; host port 3101 maps to Loki container port 3100 per D-309 | Implemented |
| R-304 | 2026-04-01 | 3.2 | Loki was only on internal monitoring network -- no host port binding for cross-host access | High | Added ports binding (D-309): `${LOKI_BIND_ADDRESS:-0.0.0.0}:${LOKI_EXTERNAL_PORT:-3101}:3100` to observability docker-compose.yml. Operator should set LOKI_BIND_ADDRESS=192.168.50.194 in .env for tighter binding |
| R-305 | 2026-04-01 | 3.2 | No .env file template in promtail-media role (unlike diun-media which has diun-media.env.j2) | Low | Promtail uses a YAML config file, not environment variables. All configuration is in promtail-config.yml.j2; only TZ is passed via docker-compose environment directive. No .env file needed |
| R-306 | 2026-04-01 | 3.2 | No proxy network in docker-compose.yml.j2 for promtail-media | Low | By design -- same as diun-media (R-607). ct-media-01 Promtail uses host IP for cross-host Loki access; default bridge network sufficient; no Traefik routing needed |
| D-628 | 2026-04-01 | 6.6 | Create validation script despite epics saying "No new files -- validation only" | 95% | Consistent with D-412 (Story 4.4) and D-217 (Story 2.4); validation script provides repeatable automated verification of all FR24-FR28 checks; matches established pattern across all validation stories | Implemented |
| D-629 | 2026-04-01 | 6.6 | 8 checks mapping 1:1 to the 8 validation items specified in the dev-story brief | 95% | Each check maps to a specific FR or cross-cutting concern; comprehensive coverage without redundancy | Implemented |
| D-630 | 2026-04-01 | 6.6 | SKIP for runtime checks (diun container health, SMTP TCP connect) when not on homelab network | 95% | Matches graceful degradation pattern from D-219 (Story 2.4) and D-413 (Story 4.4); prevents false failures on dev machine | Implemented |
| D-631 | 2026-04-01 | 6.6 | Verify Prometheus textfile metrics presence in apt-check and tool-check scripts as part of FR28 validation | 95% | FR28 requires dashboard visibility; textfile metrics are the data pipeline feeding the Grafana dashboard; validating their presence in scripts ensures end-to-end data flow | Implemented |
| R-615 | 2026-04-01 | 6.6 | ct-docker-01 Diun uses Docker service name `smtp-relay` while other sources use IP 192.168.50.194 | Low | Correct by design -- ct-docker-01 Diun is on same Docker proxy network as smtp-relay container (Docker DNS resolution); cross-host sources (apt-check, tool-check, diun-media) must use host IP |
| R-616 | 2026-04-01 | 6.6 | diun-media Ansible role env template references `diun_media_watch_by_default` but compose template does not set WATCHBYDEFAULT env var directly | Low | By design -- the .env.j2 template (not compose) sets DIUN_PROVIDERS_DOCKER_WATCHBYDEFAULT; compose uses env_file directive to load .env; separation follows Ansible best practice for secrets/config |
