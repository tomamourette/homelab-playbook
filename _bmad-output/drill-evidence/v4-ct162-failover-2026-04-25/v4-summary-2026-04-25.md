# V4 Drill Summary — graceful HA failover ct:162 via `ha-manager crm-command migrate`

**Date:** 2026-04-25
**Operator:** BMad Dev (claude-opus-4-7)
**Story:** 6.6
**Window:** 09:28–09:34 UTC (≈6 min wall-clock)
**Result:** PASS (GREEN on all timing thresholds)

## Pre-flight

- Cluster 3/3 quorate, ring 1.1c0
- 6 HA services started: ct:101 pve1, ct:151 pve2, ct:160/162/250 pve3, vm:100 pve1
- Replication 162-* on `*/1` (post-Story-6.5), FailCount=0, LastSync 1–2 s
- Alertmanager: zero PVE alerts firing
- Prometheus had a pre-drill blip: `prometheus` container exited at 09:24:30Z (exit code 255, no error in logs — clean compaction window completed). Restarted before AC-8 evidence capture; healthy throughout drill window.

## Heartbeat

Written inside ct:162 pre-drill (on pve3): `v4-drill T_pre=2026-04-25T09:28:59Z epoch=1777109339`

Survived both legs byte-identical.

## Forward leg (pve3 → pve2)

| Event | UTC time | Epoch |
|---|---|---|
| T_migrate_issued | 09:30:34 | 1777109434 |
| T_ct_shutdown (pve3 LRM) | 09:30:38 | 1777109438 |
| T_ct_started (pve2 LRM) | 09:30:49 | 1777109449 |
| T_HA_started (CRM) | 09:30:56 | 1777109456 |

- **Outage (LRM-bounded): 11 s** ← AC-3 GREEN
- Wall-clock cmd → CRM `started`: 22 s ← AC-2 GREEN
- Heartbeat readable on pve2 immediately post-start; uptime = 0 min (real restart); IP 192.168.50.162 up

## Backward leg (pve2 → pve3)

| Event | UTC time | Epoch |
|---|---|---|
| T_migrate_back_issued | 09:31:59 | 1777109519 |
| T_ct_shutdown (pve2 LRM) | 09:32:07 | 1777109527 |
| T_ct_started (pve3 LRM) | 09:32:19 | 1777109539 |
| T_HA_started (CRM) | 09:32:25 | 1777109545 |

- **Outage (LRM-bounded): 12 s** ← AC-6 GREEN
- Wall-clock cmd → CRM `started`: 26 s
- Round-trip wall-clock (cmd-fwd → cmd-back complete): 111 s ← AC-6 GREEN

## Replication direction flip (AC-4)

**Auto-flip in place** — Proxmox rewrote `source` field of 162-0 and 162-1 in `/etc/pve/replication.cfg` within one cycle, no operator intervention.

| Snapshot | 162-0 source | 162-0 target | 162-1 source | 162-1 target | LastSync delta |
|---|---|---|---|---|---|
| Pre-drill | pve3 | pve1 | pve3 | pve2 | <60 s |
| Post-fwd-migrate | **pve2** | pve1 | **pve2** | **pve3** | first cycle 11:31:01–11:31:06 |
| Post-back-migrate | pve3 | pve1 | pve3 | pve2 | first cycle 11:32:01–11:32:08 |

`FailCount=0` throughout. Cosmetic note: stale comment "HA replica to pve2" on `162-1` persisted (target field cycled correctly — `pve2 → pve3 → pve2`).

## Notification (AC-5)

**Outcome B — no HA-migration alert exists.** Confirmed and expected. Only `AlertmanagerDeadManSwitch` (info heartbeat) fired during window. Backlog note for Epic 7: optionally add `PVEClusterCTMigrated` rule on home-node label change. Does not block 6.7 — pull-plug will exercise `PVEReplicationStale` / `PVEReplicationFailing` (Story 7.11 covered).

## AC-8 Story 6.10 alert chain

`pve_ha_resource_state{sid="ct:162",state="started"}` captured both transitions via `exported_node` label mutation:

- `exported_node=pve3` present 1777109075–1777109435, absent 1777109435–1777109535, present 1777109535–1777109675
- `exported_node=pve2` present 1777109495–1777109600

`changes()` returned 0 (label-set transitions don't count as value changes in a single series — known semantic of `changes()`); the correct query is `count by (exported_node) (count_over_time(...[20m]))` which returns 3 distinct samples per home node, confirming both home-nodes were captured. `error|fence|recovery` state never non-zero — `PVEHAResourceUnhealthy` correctly silent. Alert chain integrity: PASS.

## Final disposition

**PASS — GREEN.**

- Forward outage 11 s (target ≤60 s)
- Backward outage 12 s (target ≤60 s)
- Round-trip 111 s (target ≤180 s)
- Replication auto-flip: in-place, both directions, within one cycle
- Heartbeat: survived both legs byte-identical
- HA state machine: clean `started → migrate → started`, no `error` state on either leg
- Alert chain: integrity confirmed, no false positives, gap (no migrate alert) documented

**Story 6.7 (pull-plug) decision gate: GREEN — proceed.**
